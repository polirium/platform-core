/******/ (() => { // webpackBootstrap
/*!*******************************************************!*\
  !*** ./platform/core/ui/resources/assets/js/theme.js ***!
  \*******************************************************/
/**
 * demo-theme is specifically loaded right after the body and not deferred
 * to ensure we switch to the chosen dark/light theme as fast as possible.
 * This will prevent any flashes of the light theme (default) before switching.
 */
var themeConfig = {
  "theme": "light",
  "theme-base": "gray",
  "theme-font": "sans-serif",
  "theme-primary": "blue",
  "theme-radius": "1"
};
var params = new Proxy(new URLSearchParams(window.location.search), {
  get: function get(searchParams, prop) {
    return searchParams.get(prop);
  }
});
for (var key in themeConfig) {
  var param = params[key];
  var selectedValue = void 0;
  if (!!param) {
    localStorage.setItem('tabler-' + key, param);
    selectedValue = param;
  } else {
    var storedTheme = localStorage.getItem('tabler-' + key);
    selectedValue = storedTheme ? storedTheme : themeConfig[key];
  }
  if (selectedValue !== themeConfig[key]) {
    document.documentElement.setAttribute('data-bs-' + key, selectedValue);
  } else {
    document.documentElement.removeAttribute('data-bs-' + key);
  }
}
/******/ })()
;